import { WebPartContext } from "@microsoft/sp-webpart-base";
export interface ICustomizationInReactTableProps {
  description: string;
  context: WebPartContext;
  siteUrl?: string;
  Urlvalue?: string;
}
